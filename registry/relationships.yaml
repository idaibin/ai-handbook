relationships:
  - from:
      type: product
      id: createway
    to:
      type: route
      id: content-output-system
    relation: implements

  - from:
      type: product
      id: forgeway
    to:
      type: route
      id: product-delivery-system
    relation: implements

  - from:
      type: project
      id: createway
    to:
      type: asset
      id: forgeway-creative-style-validation-v1
    relation: indexes-asset

  - from:
      type: project
      id: forgeway
    to:
      type: asset
      id: forgeway-product-ui-directions-v1
    relation: indexes-asset

  - from:
      type: project
      id: feeds-hub
    to:
      type: route
      id: content-output-system
    relation: uses

  - from:
      type: project
      id: feeds-hub
    to:
      type: route
      id: product-delivery-system
    relation: uses

# Controlled relationship verbs:
# - uses
# - implements
# - tested-by
# - indexes-asset
# - distills-into
# - depends-on
# - derived-from
