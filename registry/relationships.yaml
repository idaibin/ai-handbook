relationships:
  - from:
      type: project
      id: forgeway
    to:
      type: asset
      id: forgeway-creative-style-validation-v1
    relation: indexes-asset

  - from:
      type: project
      id: forgeway
    to:
      type: asset
      id: forgeway-product-ui-directions-v1
    relation: indexes-asset

  - from:
      type: workflow
      id: ai-creative-product-experience
    to:
      type: project
      id: forgeway
    relation: uses

# Controlled relationship verbs:
# - uses
# - implements
# - tested-by
# - indexes-asset
# - distills-into
# - depends-on
# - derived-from
