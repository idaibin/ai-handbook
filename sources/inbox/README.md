# Source Inbox

本目录保存尚未进入正式来源目录的高优先级候选。Inbox 记录只代表“已发现”，不代表已经阅读、验证或推荐。

进入正式来源目录前必须：

1. 解析 canonical identity 并去重；
2. 确定领域、来源类型和来源角色；
3. 检查官方身份、许可证和访问边界；
4. 说明它要解决的问题和预期输出；
5. 选择 `selected`、`watchlist`、`rejected` 或 `archived`。

同一地址重复提供只更新 `origins` 或重复记录，不新增唯一来源。
