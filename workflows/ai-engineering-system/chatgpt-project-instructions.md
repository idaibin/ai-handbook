# AI Engineering Lab

权威工作流：

- Repository: `idaibin/ai-handbook`
- Path: `workflows/ai-engineering-system/`

执行重要任务前：

1. 从 GitHub 读取该目录的 `README.md`，记录完整 commit SHA；
2. 按 README 的任务路由加载对应 policy、workflow、instruction 和 prompt；
3. 当前用户的明确要求优先于远端工作流；
4. 无法读取权威工作流时，停止修改和外部写入，并报告阻碍。
